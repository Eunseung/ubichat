# ubichat 랜딩페이지 개선 대화 요약

## 1. 프로젝트 방향

- 기존 내부 보고용 랜딩페이지를 실제 서비스형 랜딩페이지로 개선한다.
- 학교는 확보된 상태이므로 학생 유입을 위한 콜드 스타트 해결이 핵심이다.
- 핵심 가설: 첫 화면에 대학 리스트를 노출하면 학생 유입이 증가한다.
- 전문대학 기준으로 운영하며, 대학원은 대상에서 제외한다.
- 기존 필터는 유지하되 명칭과 목적은 `검색/필터`보다 `대학 탐색`으로 표현한다.
- 랜딩페이지 톤앤매너는 기존 디자인을 유지한다.
- 브랜드 엠블럼은 기존 것을 사용하고 로고명은 `ubichat`으로 통일한다.

## 2. 사용자·권한 정책

### 학생

- 랜딩페이지와 대학 탐색 화면의 대학 정보는 비로그인 상태에서도 공개한다.
- 대학 상세 정보 확인도 공개한다.
- 상담 신청·상담 시작 시 학생 로그인이 필요하다.
- 학생 로그인 후 기존 학생 화면의 GNB를 유지하되 대학 찾기 화면을 랜딩 기반 대학 탐색 구조와 통합한다.
- 학생 GNB:
  - 대학 탐색
  - 상담하기
  - 문서 보관함
- `문서 보관함`은 마이페이지 드롭다운 메뉴로 이동한다.
- 로그인 후 프로필 메뉴에 마이페이지 하위 메뉴와 로그아웃을 제공한다.
- 비로그인 상태에서는 `학생 로그인`과 `학교 서비스`를 별도 구조로 노출한다.
- D-021에 따라 학생 로그인 상태에서는 `학교 서비스`를 숨기고, 학생 진입은 `내 상담`과 프로필 메뉴로 제공한다.
- 축소 화면에서는 페이지 메뉴, 학생 프로필·개인 메뉴, 다국어 선택을 햄버거 하나에 통합한다.
- 학생 화면은 데스크톱·모바일 반응형 구조로 통합한다.

### 대학

- 대학 로그인은 학생 로그인과 분리한다.
- 대학 로그인 후 대학 관리자 페이지로 이동한다.
- 랜딩페이지의 `학교 서비스`는 대학 로그인 화면으로 연결한다.
- 대학 멤버도 공개 정보와 상담 채널을 수정할 수 있다.
- 대학 오너 권한은 멤버 추가다.

### 플랫폼 관리자

- 플랫폼 관리자는 별도 로그인·관리자 페이지로 분리한다.
- 관리자 로그인 시 관리자 페이지로 자동 이동한다.

## 3. 대학 탐색 구조

- 랜딩페이지는 학생 관점의 대학 탐색 서비스로 구성한다.
- 참고 서비스:
  - KoWork
  - Wanted
  - Saramin
  - UwayApply
  - Jinhak
- 랜딩의 `대학 탐색`, `전체 대학 탐색` 버튼은 학생 전용 별도 목업으로 보내지 않는다.
- 전체 대학 탐색 화면은 다음을 제공한다.
  - 검색 기능
  - 전체 대학 리스트
  - 페이지네이션
  - 대학 카드의 `대학 소개 보기`
  - 페이지 하단 고정 대학 입점 신청 섹션
- 대학 카드는 가나다순으로 정렬한다.
- 승인된 대학은 모두 노출한다.
- 관리자 화면에서는 승인 대학 전체 목록을 대학 관리 하위 화면에서 확인할 수 있어야 한다.
- 미노출 대학은 더미 데이터 3개로 별도 표시해 미노출 맥락을 설명한다.
- 대학 카드 옆 상담 가능 여부를 나타내던 초록색·회색 도형은 현재 랜딩과 학생 화면에서 주석 처리한다.

## 4. 대학 상세 플로우

- 대학 카드의 `대학 소개 보기` 클릭 시 기존 대학 상세 화면으로 이동한다.
- 대학 상세 정보는 비로그인 상태에서 공개한다.
- 우측 상담 영역의 CTA는 `학생 로그인 후 상담하기`다.
- 상담 시작 전 학생 인증이 필요하다.
- 대학 소개, 모집 안내, 상담 상태, 운영 시간, 상담 언어를 제공한다.
- 상세 페이지의 상단 헤더는 스크롤 시 고정한다.

## 5. 광고·배너 정책

### 대학 배너

- 키메시지와 대학 리스트 사이에 배치한다.
- 대학 고객사가 제공한 이미지 자체를 노출한다.
- CTA는 넣지 않는다.
- 캐러셀 구조로 다른 대학 광고도 노출한다.
- 이미지 규격:
  - PC: 1180 × 220px
  - 모바일: 328 × 160px
- 제공 이미지 순서:
  1. 삼육보건대학교
  2. 한영대학교
  3. 성운대학교
- 배너는 기존 라운드 형태를 유지한다.
- 배너 내부에 좌우 제어 버튼과 페이지네이션을 배치한다.

### 기업 배너

- `한국 생활 준비 혜택` 섹션에 단일 캐러셀 배너로 배치한다.
- 설명 문구:
  - `다양한 서비스들로 유학준비를 탄탄히 하세요..`
- 기업 광고 순서:
  1. 금융
  2. 주거
  3. 통신
- 통신·금융·생활 등 분야가 하나의 배너에서 순환한다.
- 기업 배너에도 페이지네이션과 좌우 제어 버튼을 제공한다.
- 기존 라운드 배너 스타일을 유지한다.
- 기업용 배너 포함 버전과 미포함 버전을 별도 파일로 유지한다.

### 부가 서비스

- TOPIK 등 회사 부가 서비스를 별도 구좌로 제공한다.
- 통신·금융·주거는 일반 기업 광고 구좌다.
- 대학 광고와 일반 기업 광고를 시각적·정책적으로 구분한다.

## 6. 랜딩·화면 연결

- 대학 탐색 버튼: 전체 대학 탐색 화면으로 이동한다.
- 대학 입점 신청 버튼: 로그인 화면이 아닌 대학 등록 신청 페이지로 이동한다.
- 로그인 버튼: 학생 로그인 화면으로 이동한다.
- 대학 서비스 버튼: 대학 로그인 화면으로 이동한다.
- 관리자 로그인: 관리자 페이지로 이동한다.
- 랜딩·학생·대학·관리자 페이지의 대학 데이터는 하나의 데이터 소스로 통합한다.

## 7. 문서·작업 원칙

- 원본 IA 문서는 `_archive/03_기존_루트_기획자료_20260727/`에 보존하고, 개선 문서는 프로젝트 루트에서 수정한다.
- 회원별 IA·플로우·정책을 학생, 대학, 플랫폼 관리자 기준으로 분리 검토한다.
- 한 개 작업을 마무리한 뒤 사용자 검토를 받고 다음 단계로 진행한다.
- 주요 문서:
  - `회원_스쿼드별_IA_플로우_정책.md`
  - `UniChat_IA_플로우_정책_개선안.md`

## 8. 현재 구현·배포 상태

- 랜딩·학생·대학·플랫폼 관리자 목업이 프로젝트 루트에 존재한다.
- 학생 데스크톱 화면에 반응형 구조를 적용했다.
- `student-mobile.html`은 학생 데스크톱 반응형 화면으로 연결하도록 통합했다.
- 정적 호스팅용 `index.html`을 추가했다.
- 로컬 Git 저장소와 초기 커밋은 생성했다.
- 목표 GitHub 저장소:
  - `Eunseung/unichat-landing-proposal`
- GitHub 계정 인증은 정상이나, 현재 GitHub connector에서 대상 비공개 저장소 접근 시 `404 Not Found`가 발생한다.
- GitHub connector의 private repository 권한 재연결 또는 별도 GitHub CLI 인증이 필요하다.
- Vercel 배포는 GitHub 저장소 접근 권한 해결 후 진행한다.

## 9. 미완료 작업

1. GitHub connector에 private repository 접근 권한 연결
2. 현재 작업물을 비공개 GitHub 저장소에 업로드
3. Vercel 프로젝트 생성 및 GitHub 저장소 연동
4. 배포 URL 확인
5. 데스크톱·모바일 주요 플로우 최종 검수

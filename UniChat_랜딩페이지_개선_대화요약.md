# ubichat 랜딩페이지 개선 대화 요약

작성일: 2026-07-27  
현재 작업 위치: 프로젝트 루트  
이전 작업 폴더: `랜딩페이지 개선안`

## 1. 제품 방향

- 서비스명: **ubichat**
- 랜딩페이지는 내부 보고용 목업이 아닌, 학생이 한국 전문대학을 탐색하는 공개 서비스 화면이다.
- 벤치마크 성격: Kowork, Wanted, Saramin, Uwayapply처럼 학생 유입과 탐색을 우선한다.
- 대상 학교는 전문대학교 기준이며, 대학원은 범위에서 제외한다.
- 대학 탐색·대학 소개는 비로그인 학생과 대학 관계자도 볼 수 있다.
- 대학 상담 신청·상담 채널·문서 보관함은 학생 로그인 후 이용한다.

## 2. 랜딩페이지 구조

### 기본 진입

- 최종 진입: `index.html`
- `index.html`은 `기업용_배너_포함.html`로 이동한다.
- 비교안: `기업용_배너_미포함.html`

### GNB

비로그인 상태:

- 대학 탐색
- 상담하기
- 대학 서비스
- 로그인

학생 로그인 상태:

- 대학 탐색
- 상담하기
- 학생 프로필 메뉴
- `대학 서비스`, `로그인`은 비노출
- 프로필 드롭다운: 문서 보관함, 마이페이지, 로그아웃

대학 서비스는 대학 로그인/관리자 진입용이며, 학생 로그인과 분리한다.

### 메인 콘텐츠 순서

1. 키메시지: 나에게 맞는 한국 전문대학 탐색
2. 관심 분야 선택
3. 대학 고객사 배너 캐러셀
4. 지금 상담 가능한 전문대학 카드
5. 전체 대학 탐색
6. 기업 제휴 광고 배너 캐러셀 (포함안만)
7. TOPIK 부가 서비스
8. 대학 입점 신청 CTA

### 탐색 정책

- 검색/필터 중심 표현 대신 학생의 **탐색** 흐름으로 구성한다.
- 관심 분야: 전체, 요양보호, 육성형 전문기술, 뿌리산업, 기타
- 랜딩 대학 카드는 가나다순으로 정렬한다.
- 카드의 초록색/회색 상담 상태 도트는 현재 비노출 주석 처리했다.
- `전체 대학 탐색`과 상단 `대학 탐색`은 공개 대학 탐색 페이지로 이동한다.

## 3. 배너 및 광고 정책

### 대학 고객사 배너

- 키메시지와 대학 리스트 사이에 배치한다.
- CTA 없이 이미지 자체를 노출한다.
- 캐러셀 방식으로 여러 대학 광고를 보여준다.
- 이미지 규격: PC `1180 × 220px`, 모바일 `328 × 160px`
- 현재 적용 이미지 순서:
  1. 삼육보건대학교
  2. 한영대학교
  3. 성운대학교

### 기업 제휴 광고

- 대학 고객사 배너와 별도 구좌다.
- 하나의 배너에서 분야별 광고를 캐러셀로 전환한다.
- 노출 순서: 금융, 주거, 통신
- 소개 문구: `다양한 서비스들로 유학준비를 탄탄히 하세요.`
- 기업 제휴 배너는 `기업용_배너_포함.html`에만 노출한다.

## 4. 학생 플로우

### 공개 영역

1. 랜딩 또는 공개 대학 탐색 페이지 진입
2. 대학 카드 선택
3. 대학 소개 상세 확인

### 상담 시작

1. 대학 소개 상세의 `학생 로그인 후 상담하기` 선택
2. 비로그인 학생은 `login.html`로 이동
3. 로그인 또는 회원가입 완료
4. 기존에 선택한 대학 상담 흐름으로 반환

### 로그인 및 회원가입

- 학생 로그인: `login.html`
- 학생 회원가입: `signup.html`
- 로그인 화면의 `학생 회원가입`은 기존 학생 포털 목업이 아닌 `signup.html`로 직접 이동한다.
- 회원가입은 이메일 인증 목업 코드 `123456` 완료 후 제출할 수 있다.
- 로그인 상태는 `localStorage`와 `sessionStorage`에 저장한다.
- `file://` 환경에서 `localStorage`가 제한돼도 랜딩 복귀 후 로그인 상태가 유지되도록 `sessionStorage`를 백업으로 사용한다.

### 학생 포털

- 통합 반응형 학생 포털: `student-desktop.html`
- 기존 `student-mobile.html`은 통합 학생 포털로 이동한다.
- 공개 대학 탐색은 랜딩/공개 탐색 화면이 담당한다.
- 로그인 후 학생 포털 GNB에는 대학 찾기를 중복 노출하지 않는다.
- 학생 주요 기능: 상담하기, 문서 보관함, 마이페이지

## 5. 대학·플랫폼 관리자 정책

### 대학 관리자

- 파일: `university-admin.html`
- 대학 로그인은 학생 로그인과 별도다.
- 대학 멤버도 공개 정보와 상담 채널을 수정할 수 있다.
- 대학 오너 권한은 멤버 추가다.
- 대학 입점 신청은 대학 등록 신청 화면으로 이동한다.

### 플랫폼 관리자

- 파일: `platform-admin.html`
- 승인된 모든 대학은 공개 노출한다.
- 미노출 대학은 맥락 확인용 더미 데이터 3개로 관리한다.
- 등록 대학 목록은 대학 관리 하위 페이지에서 확인하는 구조다.

## 6. 주요 파일

| 구분 | 파일 |
| --- | --- |
| 최종 랜딩 진입 | `index.html` |
| 기업 배너 포함 랜딩 | `기업용_배너_포함.html` |
| 기업 배너 미포함 랜딩 | `기업용_배너_미포함.html` |
| 공개 대학 탐색 | `university-explore.html` |
| 대학 소개 상세 | `university-detail.html` |
| 학생 로그인 | `login.html` |
| 학생 회원가입 | `signup.html` |
| 학생 포털 | `student-desktop.html` |
| 대학 관리자 | `university-admin.html` |
| 플랫폼 관리자 | `platform-admin.html` |
| 공통 랜딩 스타일 | `landing.css` |
| 학생 세션/GNB 상태 | `student-session.js` |
| 대학 데이터 | `university-data.js` |
| 광고 구좌 데이터 | `ad-slot-data.js` |

## 7. 현재 확인 필요 항목

- 실제 대학·기업 광고 이미지 교체 시 권장 규격과 텍스트 대비를 검수한다.
- 학생 로그인 후 랜딩 복귀 시 GNB에서 `대학 서비스`, `로그인`이 비노출인지 확인한다.
- 회원가입 화면에서 실제 인증 API, 개인정보 동의 문구, 국가/도시 데이터가 확정되면 목업 로직을 교체한다.
- 플랫폼 관리자에서 실제 대학 승인·미노출 전환 정책이 확정되면 더미 데이터와 권한 정책을 실제 데이터 모델에 맞춘다.

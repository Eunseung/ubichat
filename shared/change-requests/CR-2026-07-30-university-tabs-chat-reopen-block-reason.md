# Shared Change Request

- 요청 ID: CR-2026-07-30-university-tabs-chat-reopen-block-reason
- 날짜: 2026-07-30
- 요청 스쿼드: Product Owner
- Owner: 학교 PM·Designer·Developer
- 필수 검토자: 메인·학생·어드민 PM·Developer
- 근거: D-023
- 대상: `university-admin.html`, `university-data.js`, `shared/contracts/university-publication.md`, 상담 상태·감사 형식
- 변경:
  - `입학 안내 자료` PDF 전용 업로드
  - 공개 상세 탭 `id`, `title`, `content`, `order`, `enabled` 및 기숙사 사진 목록
  - 종료 채팅의 학교측 재개 이벤트와 학생 시스템 메시지
  - 차단 사유 `advertising | abuse | other`, `other` 상세 사유 필수
- 공개 렌더링: 메인은 활성 탭만 순서대로 표시한다. `기숙사 사진`은 캐러셀이다.
- 사용자 UI 수정 (2026-07-30): 운영 화면의 기본 `기숙사 소개`·`기숙사 사진`은 별도 탭으로 나누지 않고 `기숙사` 탭 하나로 관리한다. 해당 탭 편집에서 본문과 사진 첨부를 함께 제공하며, 기존 `dormitoryPhotos` 목록은 보존한다. 학교 프로토타입은 기존 `dormitory-photos` 탭 데이터를 `dormitory-intro`의 사진 영역으로 병합한다.
- 사용자 UI 수정 (2026-07-30): 대학 운영 관리에 소속구분별 `website`(HTTPS 공식 홈페이지)와 `locationAddress`(Google 지도 검색 기준 주소)를 추가한다. 학생용 대학 상세에는 공식 홈페이지와 Google 지도 바로가기 링크를 노출한다.
- 메인 UI 인계 (2026-07-30): `university-detail.html`의 공식 홈페이지 버튼은 상세 소개 하단 `대학 정보` 영역이 아니라, 대학 로고·대학명 헤더의 우측 빈 영역에 배치한다. 화면이 좁아지면 대학명 아래로 줄바꿈하며, `publicInfo.website.url`이 없으면 버튼을 노출하지 않는다. Google 지도·주소 영역은 기존 `대학 정보` 위치를 유지한다. 데이터 schema 변경은 없다.
- 메인 UI 인계 (2026-07-30): 공개 상세의 `profile.photos` 노출 명칭은 `소개 사진`이 아니라 `대학 사진`으로 통일한다. 갤러리 제목·빈 상태·사진 상세 모달 제목만 바꾸며, 사진 배열 데이터 구조와 기존 파일명은 변경하지 않는다.
- 소비자 확인: 메인·학생은 `kind: dormitory` 탭의 본문 뒤에 `dormitoryPhotos` 캐러셀을 렌더링한다. D-023의 기본 탭 5개 문구와 공통 계약 본문은 PO·필수 검토자 확인 후 갱신한다.
- 보안·권한: 배정 소속구분 관계자만 재개·차단한다. 학생 문서 원본과 메시지 본문을 공통 데이터·감사 상세에 넣지 않는다.
- 호환성: 기존 `apostille` 문서 값은 학생 화면의 `공증문서` 표기와 연결한다. 기존 종료 채팅·차단 이력은 보존한다.
- 검증: PDF 형식 오류, 탭 활성·비활성·순서, 사진 없음·1장·복수, 종료→재개 동기화, 차단 사유 조건부 검증, 감사 필드 누락, JavaScript 문법
- 사용자 사전 승인: 2026-07-30 내부 보고 수정 지시
- 사용자 최종 확인: 대기
